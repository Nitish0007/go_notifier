apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: notifier
spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
    - name: grpc
      port: 50051
      targetPort: 50051
      nodePort: 30051